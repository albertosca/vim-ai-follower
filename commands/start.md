---
description: Start the vim-ai-follower pane for this tmux window. Optional args pass through (e.g. --backend nvim --speed lento).
---

Run `claude-follow start $ARGUMENTS` in the Bash tool and report the result. Do
not add any behavior beyond invoking the CLI.
